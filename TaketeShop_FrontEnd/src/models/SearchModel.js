function SearchModel({visible, data, notFound}) {
    if (!visible) return null;
    if (visible && data.length === 0 && !notFound) return null;
    
    return (  );
}

export default SearchModel;{visible, data, notFound}